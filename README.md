# Origin of spaces

## Used packages
- `rgplates` 0.6.1 (with `GPlates` 2.5)
- `chronosphere` 0.6.1


## Instructions

- Once all code is run in `code/gloss.R` then 2 plots will be in a new directory `export`. 
- The mock-up is in `doc/figs/glossopteris.svg`, which links to the component files.
